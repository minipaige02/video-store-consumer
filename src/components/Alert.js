import React from 'react';
import PropTypes from 'prop-types';

const Alert = (props) => {


  return(
    <section>
      
    </section>
  );
};

// Alert.propTypes = {

// }

export default Alert;
